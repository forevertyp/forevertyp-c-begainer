#include<stdio.h>
int main(){
	int x;
	int count[10];

	scanf("%d",&x);
	while(x！=-1）{
		if(x>=0&&x<=9){
			count[x] ++;
		}
		scanf("%d",&x);
	}
	for(int i=0;i<10;i++){
		printf("%d的个数为%d个",i,count[i]);
	}

	return 0;
}
